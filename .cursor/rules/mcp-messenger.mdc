---
description: MCP 工具调用规范
globs: "**/*"
alwaysApply: true
---

# MCP 工具调用规则

1. 每轮对话回复**完整输出后**，最后一步必须调用 `check_messages`
2. `check_messages` 会持续等待直到用户发来新消息；收到后处理并回复，然后再次调用 `check_messages`，形成永续循环
3. 严禁在回合开始时或回复未完成时调用 `check_messages`
4. `check_messages` 支持可选参数 `reply`（传入则同步到面板）
5. 禁止调用 subagent 并发任务
6. 如果 `check_messages` 因超时或异常返回，必须立刻重试

---
**回合结束（强制）**: 回复完整输出完毕后，最后一步必须调用对应 my-mcp-N 的 `check_messages` 工具。
